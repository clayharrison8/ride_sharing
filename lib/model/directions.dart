class Directions {
  int distanceNum, durationNum;
  String distanceText, durationText, encodedPoints;

  Directions(int distanceNum, int durationNum, String distanceText, String durationText, String encodedPoints){
    this.distanceNum = distanceNum;
    this.durationNum = durationNum;
    this.distanceText = distanceText;
    this.durationText = durationText;
    this.encodedPoints = encodedPoints;

  }
}
